This is a sample setup howto provision a new VM using Vagrant and setup Smallworld using Puppet.

KNOWN ISSUES
============

 * installation media is not unmounted and prevents reboot
 * everything after the installation fails, since the task isn't finished after the exec task
